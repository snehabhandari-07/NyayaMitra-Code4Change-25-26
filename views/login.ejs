<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>NJDG | Lawyer Login</title>
</head>
<body class="bg-slate-100 h-screen flex items-center justify-center font-sans">
    <div class="bg-white p-8 rounded-2xl shadow-xl w-full max-w-md border border-slate-200">
        <div class="text-center mb-8">
            <div class="inline-block p-2 rounded mb-4">
                <img src="https://upload.wikimedia.org/wikipedia/commons/5/55/Emblem_of_India.svg" alt="Logo" class="h-12">
            </div>
            <h1 class="text-2xl font-bold text-slate-800">Lawyer Portal</h1>
            <p class="text-slate-500 text-sm italic">National Judicial Data Grid Access</p>
        </div>

        <form action="/login" method="POST" class="space-y-6">
            <div>
                <label class="block text-sm font-bold text-slate-700 mb-2">Advocate Full Name</label>
                <input type="text" name="lawyerName" required placeholder="Enter name as per court records" 
                    class="w-full px-4 py-3 border rounded-lg focus:ring-2 focus:ring-blue-500 outline-none transition-all">
                <p class="text-[10px] text-slate-400 mt-1">Note: Authentication is based on Petitioner/Respondent Advocate records.</p>
            </div>
            
            <% if (error) { %>
                <p class="text-red-500 text-xs font-bold text-center"><%= error %></p>
            <% } %>

            <button type="submit" class="w-full bg-blue-900 text-white font-bold py-3 rounded-lg hover:bg-blue-800 transition-colors shadow-lg">
                ENTER DASHBOARD
            </button>
        </form>
        
        <div class="mt-8 text-center border-t pt-4">
            <p class="text-[10px] text-slate-400 uppercase tracking-widest font-bold">Authorized Access Only</p>
        </div>
    </div>
</body>
</html>